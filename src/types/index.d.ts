export interface Video {
    category: string
    id: string
    title: string
    description: string
    views: number
    uploadDate: string
    videoUrl: string
    thumbnail: string
  }

  export interface TrendingVideo {
    id: string;
    title: string;
    views: number;
    thumbnail: string;
  }